# Database Configuration
# PostgreSQL connection string from Neon (https://neon.tech)
# Format: postgresql://user:password@host/database?sslmode=require
DATABASE_URL='postgresql://user:password@host.neon.tech/database?sslmode=require'

# Vite requires VITE_ prefix for client-side access
# This should typically be the same as DATABASE_URL
VITE_DATABASE_URL='postgresql://user:password@host.neon.tech/database?sslmode=require'

# Stack Auth Configuration (https://docs.stack-auth.com)
# Stack Auth provides authentication and user management
# Get these values from your Stack Auth dashboard

# Unique identifier for your Stack Auth project
STACK_PROJECT_ID='your-project-id-here'

# Public key for client-side authentication (safe to expose)
STACK_PUBLISHABLE_CLIENT_KEY='pck_your_publishable_key_here'

# Secret key for server-side authentication (keep secure, never expose to client)
STACK_SECRET_SERVER_KEY='ssk_your_secret_key_here'

# Vercel Blob Storage Configuration (https://vercel.com/docs/storage/vercel-blob)
# Required for file uploads (images for hikes and camping sites)
# Get these from your Vercel dashboard under Storage > Blob

# Unique identifier for your Blob storage instance
VERCEL_BLOB_STORE_ID='your-blob-store-id'

# Authentication token for Blob storage (keep secure)
VERCEL_BLOB_TOKEN='your-blob-token-here'

# Application Environment
# Use 'development' for local development, 'production' for deployed environments
NODE_ENV='development'

# Optional: Application URL
# Used for generating absolute URLs in emails, redirects, etc.
# PUBLIC_APP_URL='http://localhost:5173'
